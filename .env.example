# Stream Pay Integration Environment Variables

# IMPORTANT: Authentication uses BOTH API Key and Secret Key
# You need BOTH values from your Stream Pay dashboard

# Stream Pay API Configuration
# Get these from your Stream Pay dashboard → API Settings
STREAM_PAY_API_URL=https://stream-app-service.streampay.sa/api/v2
STREAM_PAY_API_KEY=your_api_key_here
STREAM_PAY_SECRET_KEY=your_secret_key_here

# Application Configuration
APP_URL=http://localhost:3000

# Note: The API uses x-api-key header with Base64(api-key:api-secret) encoding
# Example: echo -n 'api-key:api-secret' | base64
